# Frontend — NutriWatch Dashboard

The React dashboard source lives in the repo root (`/src`, `/public`, etc.) per the TanStack Start template. This folder is reserved for frontend-specific docs, design assets, and future extraction into a dedicated package.
